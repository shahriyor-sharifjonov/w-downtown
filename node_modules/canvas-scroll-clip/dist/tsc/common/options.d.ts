import { IUserInputs, IFrame } from '../helpers/intefaces';
import Base from './base';
/**
 * Plugin options.
 *
 * @export
 * @class Options
 * @implements {IOptions}
 */
export declare class Options extends Base {
    inputs: IUserInputs;
    identifier: string;
    scrollArea: number;
    protected frame: IFrame;
    /**
     * Creates an instance of Options.
     * @param {IUserInputs} [options] Options to copy properties.
     * @memberof Options
     */
    constructor(options: IUserInputs);
    /**
     * Set scrollable area height
     */
    set setScrollableArea(height: number);
}
/**
 * @export
 */
export default Options;
